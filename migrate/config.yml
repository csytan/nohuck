# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Comment
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 14 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: group
      external_name: group
      # Type: Key Stats: 14 properties of this type in this kind.
      import_transform: transform.key_id_or_name_as_string
      export_transform: transform.key_id_or_name_as_string

    - property: points
      external_name: points
      # Type: Integer Stats: 14 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: reply_to
      external_name: reply_to
      # Type: Key Stats: 1 properties of this type in this kind.
      import_transform: transform.key_id_or_name_as_string
      export_transform: transform.key_id_or_name_as_string

    - property: text
      external_name: text
      # Type: Text Stats: 14 properties of this type in this kind.
      import_transform: db.Text

    - property: updated
      external_name: updated
      # Type: Date/Time Stats: 14 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: user
      external_name: user
      # Type: Key Stats: 14 properties of this type in this kind.
      import_transform: transform.key_id_or_name_as_string
      export_transform: transform.key_id_or_name_as_string

    - property: video
      external_name: video
      # Type: Key Stats: 14 properties of this type in this kind.
      import_transform: transform.key_id_or_name_as_string
      export_transform: transform.key_id_or_name_as_string


- kind: Counter
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: i
      external_name: i
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Feed
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: bulk_add
      external_name: bulk_add
      # Type: Boolean Stats: 7 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: created
      external_name: created
      # Type: Date/Time Stats: 18 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: feed_url
      external_name: feed_url
      # Type: String Stats: 8 properties of this type in this kind.

    - property: group
      external_name: group
      # Type: Key Stats: 18 properties of this type in this kind.
      import_transform: transform.key_id_or_name_as_string
      export_transform: transform.key_id_or_name_as_string

    - property: last_vid_added
      external_name: last_vid_added
      # Type: Date/Time Stats: 10 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: name
      external_name: name
      # Type: Text Stats: 18 properties of this type in this kind.
      import_transform: db.Text

    - property: next_update
      external_name: next_update
      # Type: Date/Time Stats: 18 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: title
      external_name: title
      # Type: String Stats: 7 properties of this type in this kind.

    - property: updated
      external_name: updated
      # Type: Date/Time Stats: 18 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: url
      external_name: url
      # Type: Text Stats: 18 properties of this type in this kind.
      import_transform: db.Text


- kind: Group
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: about
      external_name: about
      # Type: Text Stats: 1 properties of this type in this kind.
      import_transform: db.Text

    - property: categories
      external_name: categories
      # Type: String Stats: 13 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 3 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: domain
      external_name: domain
      # Type: String Stats: 2 properties of this type in this kind.

    - property: facebook_id
      external_name: facebook_id
      # Type: Text Stats: 2 properties of this type in this kind.
      import_transform: db.Text

    - property: like_word
      external_name: like_word
      # Type: Text Stats: 6 properties of this type in this kind.
      import_transform: db.Text

    - property: likes_word
      external_name: likes_word
      # Type: Text Stats: 6 properties of this type in this kind.
      import_transform: db.Text

    - property: motd
      external_name: motd
      # Type: Text Stats: 1 properties of this type in this kind.
      import_transform: db.Text

    - property: name
      external_name: name
      # Type: String Stats: 2 properties of this type in this kind.

    - property: tagline
      external_name: tagline
      # Type: Text Stats: 6 properties of this type in this kind.
      import_transform: db.Text

    - property: tags
      external_name: tags
      # Type: Blob Stats: 6 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode


- kind: User
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: about
      external_name: about
      # Type: Text Stats: 3 properties of this type in this kind.
      import_transform: db.Text

    - property: created
      external_name: created
      # Type: Date/Time Stats: 73 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: group_karma
      external_name: group_karma
      # Type: Blob Stats: 73 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: groups
      external_name: groups
      # Type: String Stats: 61 properties of this type in this kind.

    - property: karma
      external_name: karma
      # Type: Integer Stats: 73 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: password
      external_name: password
      # Type: Text Stats: 73 properties of this type in this kind.
      import_transform: db.Text

    - property: updated
      external_name: updated
      # Type: Date/Time Stats: 73 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: Video
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 4349 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: feed
      external_name: feed
      # Type: Key Stats: 1640 properties of this type in this kind.
      import_transform: transform.key_id_or_name_as_string
      export_transform: transform.key_id_or_name_as_string

    - property: group
      external_name: group
      # Type: Key Stats: 4349 properties of this type in this kind.
      import_transform: transform.key_id_or_name_as_string
      export_transform: transform.key_id_or_name_as_string

    - property: hot
      external_name: hot
      # Type: Boolean Stats: 4349 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: ip_likes
      external_name: ip_likes
      # Type: Text Stats: 2045 properties of this type in this kind.
      import_transform: db.Text

    - property: n_comments
      external_name: n_comments
      # Type: Integer Stats: 4349 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: points
      external_name: points
      # Type: Integer Stats: 4349 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: review
      external_name: review
      # Type: Boolean Stats: 366 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: score
      external_name: score
      # Type: Float Stats: 4349 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: suggested_tags
      external_name: suggested_tags
      # Type: Text Stats: 4371 properties of this type in this kind.
      import_transform: db.Text

    - property: tags
      external_name: tags
      # Type: String Stats: 4750 properties of this type in this kind.

    - property: text
      external_name: text
      # Type: Text Stats: 4349 properties of this type in this kind.
      import_transform: db.Text

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: thumbnail
      external_name: thumbnail
      # Type: Text Stats: 2250 properties of this type in this kind.
      import_transform: db.Text

    - property: title
      external_name: title
      # Type: String Stats: 4349 properties of this type in this kind.

    - property: top_score
      external_name: top_score
      # Type: Float Stats: 4349 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: updated
      external_name: updated
      # Type: Date/Time Stats: 4349 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: user
      external_name: user
      # Type: Key Stats: 117 properties of this type in this kind.
      import_transform: transform.key_id_or_name_as_string
      export_transform: transform.key_id_or_name_as_string

    - property: user_dislikes
      external_name: user_dislikes
      # Type: Text Stats: 20 properties of this type in this kind.
      import_transform: db.Text

    - property: user_likes
      external_name: user_likes
      # Type: String Stats: 229 properties of this type in this kind.

    - property: video_ids
      external_name: video_ids
      # Type: String Stats: 4424 properties of this type in this kind.

    - property: video_type
      external_name: video_type
      # Type: String Stats: 4349 properties of this type in this kind.


